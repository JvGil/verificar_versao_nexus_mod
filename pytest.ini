[pytest]
minversion = 7.0
addopts = -ra -q
testpaths = tests
# Enable live CLI logging so INFO messages from tests are visible
log_cli = true
log_cli_level = INFO
# Show only the message in logs (no timestamp, no level)
log_cli_format = %(message)s
log_format = %(message)s
